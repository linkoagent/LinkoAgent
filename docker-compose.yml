services:
  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: linko
      POSTGRES_PASSWORD: linko
      POSTGRES_DB: linko
    ports:
      - "5432:5432"
    volumes:
      - linko_pgdata:/var/lib/postgresql/data

volumes:
  linko_pgdata:
